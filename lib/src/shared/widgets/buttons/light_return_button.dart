import 'package:flutter/material.dart';

class LightReturnButton extends StatelessWidget {
  const LightReturnButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
