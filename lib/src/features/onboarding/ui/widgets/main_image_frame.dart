import 'package:flutter/material.dart';

class MainImageFrame extends StatelessWidget {
  const MainImageFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
